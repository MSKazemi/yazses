# examples/config.example.toml
# Copy to ~/.config/yazses/config.toml and edit as needed.

[stt]
model = "tiny.en"         # tiny.en (fast) | base.en (accurate)
device = "cpu"
compute_type = "int8"

[hotkey]
key = "auto"
hold_threshold_ms = 500

[audio]
sample_rate = 16000
max_record_seconds = 90

[injection]
backend = "auto"           # auto | type | ydotool | wtype | clipboard
                           # "xdotool" is not a recognised value: on X11 `auto`
                           # already selects xdotool, and on Wayland it is ignored.
fallback_to_clipboard = true

[general]
log_level = "INFO"

[overlay]
enabled = true             # on by default; sonar rings near the cursor when you speak (needs the `overlay` extra / PySide6)
style = "sonar"            # sonar | dot
position = "cursor"        # cursor | fixed
