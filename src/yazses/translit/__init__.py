"""Romanized → Native-Script Transliteration (ADR-v2-116).

Dictate a native language in Latin letters and inject the native script. The ``scheme`` module is
the pure table-driven transliterator + scheme gate. OFF by default.
"""
