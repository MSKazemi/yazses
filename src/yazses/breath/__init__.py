"""Breath-Paced Dictation (ADR-v2-099).

Segment dictation by natural breath groups (inhalation onsets), not silence. The ``segment`` module
is the pure envelope + onset detector + segmenter (pure numpy). OFF by default.
"""
