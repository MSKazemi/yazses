"""Semantic Line Breaks (ADR-v2-111).

Break dictated prose one clause per source line so git diffs stay clean (rendered output unchanged).
The ``breaks`` module is the pure transformation. OFF by default.
"""
