"""Spoken Regex Builder (ADR-v2-066).

Build regexes/search patterns by voice. The ``build`` module is the pure compositional grammar;
an optional model for free-form descriptions is deferred. OFF by default.
"""
