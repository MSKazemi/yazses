"""Semantic Auto-Stop — hands-free tap-and-speak (ADR-v2-029).

An alternative to hold-to-talk for users who can't hold a key: tap to start, auto-stop when
the sentence is finished. The ``autostop`` module is the pure timeout/cap decision; the
Smart Turn v2 semantic model is opt-in behind the ``turn`` extra. OFF by default.
"""
