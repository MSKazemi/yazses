"""Vocal Morse — two-tone timing-coded AAC (ADR-v2-105).

Decode timed vocal pulses (short/long) into text via international Morse. The ``decode`` module is
the pure timing classifier + decoder + adaptive calibration; pulse timing comes from the existing
hold/mouth-sound stream. OFF by default.
"""
