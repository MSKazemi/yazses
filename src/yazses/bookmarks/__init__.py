"""Session Bookmarks & Resume (ADR-v2-090).

Drop named anchors in a long dictation session and re-navigate by voice. The ``store`` module is
the pure bookmark store + command grammar. OFF by default.
"""
