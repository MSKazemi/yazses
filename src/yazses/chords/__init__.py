"""Chorded Shortcut Synthesis (ADR-v2-085).

Parse an arbitrary spoken modifier+key chord and inject it — no pre-registration. The ``parse``
module is the pure grammar; injection reuses the existing ``inject_key_sequence``. OFF by default.
"""
