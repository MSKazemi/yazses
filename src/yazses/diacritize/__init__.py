"""Diacritize — diacritics restoration on dictated words (ADR-v2-122).

Restore diacritics ASR drops ("cafe" → "café", "naive" → "naïve"). The ``restore`` module is the
pure lexicon-driven restorer; neural diacritizers stay optional backends. OFF by default.
"""
