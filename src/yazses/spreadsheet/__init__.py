"""Spoken Spreadsheet / Table Mode (ADR-v2-059).

Cell-addressed dictation + grid navigation for spreadsheets/tables. The ``grid`` module is the
pure movement→keystroke + cell-reference parser, extending the commands/grammar → dispatch →
injector path. OFF by default.
"""
