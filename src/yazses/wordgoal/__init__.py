"""Word-Count & Writing-Goal Tracker (ADR-v2-092).

Count YazSes-injected words, set a goal, and answer progress queries by read-back. The ``tracker``
module is the pure counter + goal grammar; the spoken answer reuses the existing TTS. OFF by default.
"""
