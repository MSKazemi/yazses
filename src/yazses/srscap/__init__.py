"""Spoken Spaced-Repetition Capture (ADR-v2-112).

Turn "remember that X is Y" into an Anki cloze card, scheduled by SM-2. The ``cards`` module is the
pure cue-detector + cloze + scheduler. OFF by default.
"""
