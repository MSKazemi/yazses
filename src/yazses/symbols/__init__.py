"""Emoji & Symbol by Voice (ADR-v2-055).

Speak emoji, symbols, and arrows into their Unicode characters. The ``lookup`` module is the
pure name→codepoint table + phrase replacement; descriptive fuzzy lookup is deferred. OFF by
default (names overlap ordinary speech).
"""
