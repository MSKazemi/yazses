"""Voice Case & Identifier Transform on Selection (ADR-v2-087).

Transform the current selection between case/identifier conventions by voice. The ``transform``
module is the pure tokenizer + renderers + command detector; it uses the existing clipboard bridge.
OFF by default.
"""
