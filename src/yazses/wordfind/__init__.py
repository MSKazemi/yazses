"""WordFind — offline reverse dictionary (ADR-v2-118).

"The word for when water turns to gas" → a ranked shortlist (evaporation, …) for tip-of-the-tongue
and anomia moments. The ``rank`` module is the pure overlap ranker; WordNet-scale lexicons and
embedding rerankers stay optional extras. OFF by default.
"""
