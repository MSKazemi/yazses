"""Voice Snippets — spoken text expander (ADR-v2-026).

Say a trigger phrase ("insert my signature") and inject a stored template. Distinct from
`[macros]` (keystrokes) and `vocabulary` (recognition bias). The ``expand`` module is the
pure match/expand core; entries live in the local config. OFF by default.
"""
