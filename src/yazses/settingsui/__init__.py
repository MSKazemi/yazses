"""Settings GUI — a registry-driven PySide6 window for `yazses settings`.

Split the same way as ``yazses.tray``: ``model.py`` and ``controller.py`` are
pure and unit-testable without Qt; ``app.py`` is the thin Qt shell.
"""
