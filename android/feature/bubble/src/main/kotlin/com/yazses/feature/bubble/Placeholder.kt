package com.yazses.feature.bubble

/** Placeholder so the module configures before its issue lands: opt-in floating mic overlay. */
internal object Placeholder {
    const val MODULE: String = "feature/bubble"
}
