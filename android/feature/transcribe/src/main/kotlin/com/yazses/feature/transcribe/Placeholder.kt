package com.yazses.feature.transcribe

/** Placeholder so the module configures before its issue lands: SAF file import to transcript, share-sheet target. */
internal object Placeholder {
    const val MODULE: String = "feature/transcribe"
}
