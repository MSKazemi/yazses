package com.yazses.feature.meeting

/** Placeholder so the module configures before its issue lands: foreground service, live transcript, finalize. */
internal object Placeholder {
    const val MODULE: String = "feature/meeting"
}
