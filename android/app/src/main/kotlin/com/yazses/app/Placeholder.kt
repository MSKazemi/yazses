package com.yazses.app

/** Placeholder so the module configures before its issue lands: assembly, onboarding, permissions, About. */
internal object Placeholder {
    const val MODULE: String = "app"
}
