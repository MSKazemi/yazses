package com.yazses.bench

/** Placeholder so the module configures before its issue lands: on-device benchmark harness, emits device-report JSON. */
internal object Placeholder {
    const val MODULE: String = "bench"
}
