package com.yazses.jni.sherpaonnx

/** Placeholder so the module configures before its issue lands: sherpa-onnx engine, VAD, diarization (optional flavour). */
internal object Placeholder {
    const val MODULE: String = "native/sherpaonnx"
}
