org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# AndroidX is required by every modern Android library; declaring it here rather
# than per-module keeps the Android build files down to what is actually specific.
android.useAndroidX=true
kotlin.code.style=official
