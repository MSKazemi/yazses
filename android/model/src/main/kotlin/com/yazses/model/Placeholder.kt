package com.yazses.model

/** Placeholder so the module configures before its issue lands: the ONLY module with INTERNET — catalogue, download, SHA-256 verify. */
internal object Placeholder {
    const val MODULE: String = "model"
}
