package com.yazses.platform.audio

/** Placeholder so the module configures before its issue lands: AudioRecord capture, device selection, audio-focus handling. */
internal object Placeholder {
    const val MODULE: String = "platform/audio"
}
