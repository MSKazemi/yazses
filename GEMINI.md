# Gemini CLI entrypoint

@./AGENTS.md

This file intentionally contains no separate project rules. `AGENTS.md` is the canonical
YazSes instruction file for coding agents; this adapter exists only so Gemini CLI loads it
automatically as project context.

Update `AGENTS.md`, not this adapter, when project instructions change.
